database:
  username: ${TEST_USERNAME:gustavo}
  password: ${TEST_PASSWORD:3030}
  url: ${TEST_URL:localhost:3203}

spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem::dbuser
    username: sa
  flyway:
    enabled: false